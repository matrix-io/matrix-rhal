# "Legacy" build file when using `make`.
# Also see `CMakeLists.txt`.

# Binary output.
# Will create `build/PROJECT_NAME.bin`.
# If change this need to update:
# - `install.sh`
PROJECT_NAME := esp-app

include $(IDF_PATH)/make/project.mk
